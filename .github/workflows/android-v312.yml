name: Prism Hop Android V3.12.0
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: ['.github/v3120/**','.github/workflows/android-v312.yml']
permissions: {contents: read, actions: read}
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Restore V3.11 baseline
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          curl --fail --location -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/10498928541/zip" -o v311.zip
          unzip -q v311.zip -d v311 && unzip -q v311/Prism-Hop-V3.11.0-Android-Source.zip -d src && cp -R src/android ./android
      - name: Apply First Spark 2.5D
        run: |
          cat .github/v3120/chunk_0{0,1,2,3} > /tmp/v312.b64
          python3 - <<'PY'
          import base64,gzip
          from pathlib import Path
          code=gzip.decompress(base64.b64decode(Path('/tmp/v312.b64').read_text())).decode().replace("Path('/mnt/data/prism312/android')","Path('android')")
          exec(compile(code,'.github/v3120/patch.py','exec'))
          PY
          grep -q 'versionCode 46' android/app/build.gradle
          grep -q '3.12.0-android' android/app/src/main/assets/index.html
          grep -q 'story-cinematic-v312' android/app/src/main/assets/index.html
          grep -q 'storyDrawDeathFx312' android/app/src/main/assets/index.html
          python3 - <<'PY'
          from pathlib import Path
          import re,subprocess,tempfile
          s=Path('android/app/src/main/assets/index.html').read_text()
          for i,js in enumerate(re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',s,re.S|re.I)):
            p=Path(tempfile.gettempdir())/f'p312-{i}.js';p.write_text(js);subprocess.run(['node','--check',str(p)],check=True)
          PY
          zip -qr Prism-Hop-V3.12.0-Android-Source.zip android -x 'android/.gradle/*' 'android/app/build/*'
      - uses: actions/setup-java@v5
        with: {distribution: temurin, java-version: '17'}
      - uses: gradle/actions/setup-gradle@v4
        with: {gradle-version: '8.13'}
      - name: Restore keystore
        env: {KEYSTORE_BASE64: '${{ secrets.PRISM_HOP_KEYSTORE_BASE64 }}'}
        run: printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/prism-hop-upload.jks"
      - name: Build
        run: gradle -p android :app:assembleDebug :app:bundleRelease --stacktrace
      - name: Sign
        env:
          STORE_PASS: '${{ secrets.PRISM_HOP_KEYSTORE_PASSWORD }}'
          KEY_PASS: '${{ secrets.PRISM_HOP_KEY_PASSWORD }}'
          KEY_ALIAS: '${{ secrets.PRISM_HOP_KEY_ALIAS }}'
        run: |
          jarsigner -sigalg SHA256withRSA -digestalg SHA-256 -keystore "$RUNNER_TEMP/prism-hop-upload.jks" -storepass "$STORE_PASS" -keypass "$KEY_PASS" android/app/build/outputs/bundle/release/app-release.aab "$KEY_ALIAS"
          jarsigner -verify android/app/build/outputs/bundle/release/app-release.aab
          cp android/app/build/outputs/apk/debug/app-debug.apk Prism-Hop-V3.12.0-First-Spark-Test.apk
          cp android/app/build/outputs/bundle/release/app-release.aab Prism-Hop-V3.12.0-Play-Signed.aab
          sha256sum Prism-Hop-V3.12.0-First-Spark-Test.apk Prism-Hop-V3.12.0-Play-Signed.aab Prism-Hop-V3.12.0-Android-Source.zip > Prism-Hop-V3.12.0-SHA256.txt
      - uses: actions/upload-artifact@v4
        with:
          name: Prism-Hop-V3.12.0-First-Spark
          path: |
            Prism-Hop-V3.12.0-First-Spark-Test.apk
            Prism-Hop-V3.12.0-Play-Signed.aab
            Prism-Hop-V3.12.0-Android-Source.zip
            Prism-Hop-V3.12.0-SHA256.txt
          retention-days: 14
